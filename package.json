{
  "name": "coupon-deals-platform",
  "private": true,
  "scripts": {
    "dev": "docker-compose -f infra/docker/docker-compose.yml up --build",
    "down": "docker-compose -f infra/docker/docker-compose.yml down",
    "logs": "docker-compose -f infra/docker/docker-compose.yml logs -f"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.1.0",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
